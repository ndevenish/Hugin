external program executor

 Pure virtual, receive commandline process, execute, and answer return value. Implementations in std/GUI.

- getNewExternalProcess
- executeExternalProcess:


external program 

 Pure virtual. New instance should be produced by executor, set the custom stuff and descriptor and execute with executor.

- setCommand:
- setArguments:

- addArgument:
- addArguemntSafely:
- addFilenameSafely:

+ quoteArgument:
+ quoteFilename:

- start
- getExitCode